my $STAT_DB = 'spicers_stat'; #Name der Stat Datenbank
my $STAT_DB_USER = 'root';    #Username für zugriff auf DB
my $STAT_DB_PASS = '';        #passwort für zugriff auf DB

my $LOG_TABLE = 'import_log'; #name der logging tabelle

my $STAT_STARTDIR = 'G:/win_data/projekte/spicers';     #wo liegen die daten der anwendung
my $STAT_SAVEDIR = 'save';                              #wo werden die eingelesenen files abgelegt

my $FOC_IMPORTDIR = 'daten-focus';                      #wo liegen die focus daten
my $FOC_TABLENAME = 'focus_data';                       #in welche tabelle kommen die focus daten
